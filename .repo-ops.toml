# tree-sitter-grammars/tree-sitter-markdown dropped the Go bindings
# (bindings/go) starting v0.5.2 — upstream removed Go support, not a
# transient constraint. Held at the last Go-compatible release.
[[hold]]
package = "github.com/tree-sitter-grammars/tree-sitter-markdown"
max_version = "0.5.1"
until = "2027-09-01"
ecosystem = "go"
reason = "v0.5.2+ removed bindings/go upstream; go build breaks above v0.5.1"
